version: '3'
services:
  server_restarter:
    container_name: server_restarter
    image: danacr/server-restarter:latest
    volumes:
      - /dev/hidraw0:/dev/hidraw0:rw
    restart: unless-stopped
    ports:
      - 80:80
